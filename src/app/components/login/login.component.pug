.col-md-4.col-md-offset-4.col-sm-6.col-sm-offset-3.login-wrapper
  .panel.panel-default
    .panel-heading
      h2.text-center.
        Iniciar sesión
    .panel-body
      form(name="form", (ngSubmit)="login()", #form="ngForm" novalidate)
        .form-group
            label(for="email").
              Email
            input.form-control(type="text", name="email", [(ngModel)]="model.email", #email="ngModel" required, placeholder="Email")
        .form-group
          label(for="password").
            Password
          input.form-control(type="password", name="password", [(ngModel)]="model.password", #password="ngModel" required, placeholder="Contraseña")
        button.btn.btn-primary([disabled]="loading").
          Iniciar sesión
        .login-form-with-errors(*ngIf="wrongSubmit").
          Email y/o contraseña incorrectos


