import { Component } from '@angular/core';

@Component({
  selector: 'spinner',
  templateUrl: './spinner.component.pug',
  styles: [require('./spinner.component.scss').toString()]
})
export class SpinnerComponent { }